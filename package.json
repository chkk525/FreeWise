{
  "name": "freewise",
  "private": true,
  "devDependencies": {
    "tailwindcss": "^3.4.10"
  },
  "scripts": {
    "build:css": "mkdir -p /tmp/freewise-tailwind && npm exec --prefix /tmp/freewise-tailwind --yes --package tailwindcss@3.4.19 -- tailwindcss -c tailwind.config.js -i app/static/css/input.css -o app/static/css/tailwind.css --minify"
  }
}
